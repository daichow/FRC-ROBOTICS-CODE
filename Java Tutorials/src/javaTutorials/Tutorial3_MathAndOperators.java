package javaTutorials;

public class Tutorial3_MathAndOperators {

	// Math Operators
	// +, -, *, /, %, Math.abs(), Math.pow()
	// These math operators work the same way in code
	// Ex. 3 + 3
	// Ex. variableName + anotherVariableName
	
	// Assignment / Increment Operators
	// +=, -=, *=, /=, ++, --
	// These operators take the left value, use the operator on it with the right value, and puts it back into the left variable
	// Ex. variableName += 5;
	// This line of code would be equivalent to variableName = variableName + 5;
	// Ex. variableName++;
	// This line of code would be equivalent to variableName = varaibleName + 1;
	
	public static void main(String[] args) {
		// Print out the value of an expression to the console
		// Increment the value of a variable, then print it out
	}
}